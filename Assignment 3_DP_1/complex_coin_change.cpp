#include<bits/stdc++.h>
using namespace std;
int arr[30];
int min_coins(int i, int k)
{
    int res1 = 
}

int main()
{
    int n, k;
    cin >> n >> k;

    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    cout << min_coins(n-1, k);
    
}